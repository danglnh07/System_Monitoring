# System monitoring service

A web version of the System monitoring project. This include a server to host the UI, and HTTP endpoint to receive data
from the sys-check tool for displaying

Please note that this service is not fully support in Windows. It's best to run it in Linux platform